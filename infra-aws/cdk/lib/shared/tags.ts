/**
 * Global AWS resource tags applied across NovaSafe stacks.
 *
 * TODO: export standard tag map (Project, Environment, Owner, CostCenter, etc.)
 * and a helper to merge tags onto CDK constructs.
 */
