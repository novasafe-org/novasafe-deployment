# Scripts

Helper scripts for CDK bootstrap, deploy orchestration, environment validation, and migration utilities.

No scripts are implemented yet. The existing Docker/Nginx deployment scripts under `opt/novasafe-deployment/` and the repository root are unchanged.
