# Domains

> **Placeholder** — This document will describe DNS and domain routing for the serverless stack.

Future contents will cover:

- **Cloudflare** as the DNS provider (unchanged from current setup)
- Subdomain mapping (API, auth, app, landing, admin, etc.)
- SSL/TLS termination and certificate strategy on AWS vs Cloudflare
- Traffic splitting during incremental migration (e.g. weighted or path-based routing)
- Alignment with existing domain documentation in the repo root (e.g. `MOBILE_API_DOMAIN.md`)
