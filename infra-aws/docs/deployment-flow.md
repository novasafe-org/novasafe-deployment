# Deployment Flow

> **Placeholder** — This document will describe how infrastructure and application changes are deployed to AWS.

Future contents will cover:

- CDK synth, diff, and deploy workflows
- CI/CD integration (GitHub Actions or equivalent)
- Environment promotion (dev → staging → production)
- Rollback and drift detection
- Coordination with the existing Docker/Nginx deployment during incremental migration
