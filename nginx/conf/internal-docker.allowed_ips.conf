# Allowed IPs/CIDRs for internal-docker.novasafe.io (Portainer).
# Add one "allow" per line for your office/VPN/admin IPs, then "deny all".
#
# Examples (uncomment and adjust):
#   allow 203.0.113.10;
#   allow 198.51.100.0/24;
#   allow 10.0.0.0/8;
#
# Option A — Testing: allow everyone (insecure). Option B — Production: comment out
# "allow all;", add lines like "allow 1.2.3.4;" for your IPs, then uncomment "deny all;".
allow all;
# deny all;


allow 66.159.203.40;