# Portainer IP allowlist — included only from portainer.conf (not conf.d/*.glob).
#
# TEMP: allow all for testing — restore corporate VPN-CIDR when done:
#   allow 66.159.204.0/24;
#   deny all;
allow all;
