# Default: deny all until you add your IP on the server.
# See internal-docker.allowed_ips.conf.example
#
# After editing on the VPS, run: ./deploy.sh nginx-reload
# This file is preserved across deploy syncs.

deny all;
